{
  "name": "@banpick/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "One Durable Object per match (D8) and one per tournament (D37). The authority for all dice, all legality, and every bracket advancement.",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@banpick/bracket": "0.1.0",
    "@banpick/engine": "0.1.0",
    "@banpick/loader": "0.1.0",
    "@banpick/types": "0.1.0"
  }
}
